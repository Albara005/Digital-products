import { NotFoundView } from "@/components/store/not-found-view";

// notFound() from storefront pages renders here, inside the store layout.
export default function StoreNotFound() {
  return <NotFoundView />;
}
